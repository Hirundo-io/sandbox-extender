# Use TypeScript on Bun for the Policy Core

The Policy Core is implemented in TypeScript on Bun and uses Cedar's WebAssembly interface. This keeps policy-adjacent code readable and aligns with MCP integration while providing a fast local runtime; Node compatibility is not a requirement for the initial release.
