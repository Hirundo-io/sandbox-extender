# Use a personal Git repository as the policy source of truth

Sandbox Extender stores rules, groupings, profiles, context lookups, and tests in the user's local `.agents/sandbox-extender` Git repository. The repository may be pushed to a remote clone for replication, because reviewable history and portable, user-owned policy matter more than a central policy service or repository-local copies.
